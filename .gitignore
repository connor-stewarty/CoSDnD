.DS_Store
Thumbs.db

# Raw audio if Vibe exports it alongside transcripts
*.mp3
*.wav
*.m4a
*.aac

# Editor/OS cruft
*.swp
.vscode/

# Local cleanup scratch space (stale git lock/tmp files the bridge couldn't unlink)
/_to_delete/

# Impeccable design-hook scratch (regenerated per session)
.impeccable/hook.cache.json
