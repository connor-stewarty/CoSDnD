# Transcripts

Raw Vibe transcripts land here, one file per session (e.g. `session-01-raw.txt`). These are source material, not a deliverable — don't worry about cleaning them up. The real recap writing happens in `../sessions/`, pulling out just what mattered.

If a transcript file gets huge, that's fine — this folder is just storage, it doesn't feed into the showcase site directly.
